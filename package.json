{
  "name": "impa-pdf-search",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "deploy": "vercel --prod",
    "deploy:preview": "vercel",
    "build:standalone": "next build && next export",
    "build:cloudflare": "cp next.config.cloudflare.ts next.config.ts && next build && cp _headers _redirects out/",
    "deploy:cloudflare": "npm run build:cloudflare && echo 'Build completed. Upload the /out directory to Cloudflare Pages.'"
  },
  "dependencies": {
    "lucide-react": "^0.544.0",
    "next": "15.5.3",
    "pdfjs-dist": "^3.11.174",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.3",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}